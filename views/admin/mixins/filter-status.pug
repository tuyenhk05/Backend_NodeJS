mixin filter-status(listStatus)
   each status in listStatus
              button(
                  button-status = status.status
                  class=`btn btn-sm ml-1 btn-outline-success ${status.class}`
              ) #{status.name}